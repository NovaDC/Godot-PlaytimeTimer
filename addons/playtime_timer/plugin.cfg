[plugin]

name = "PlaytimeTimer"
description = "A count up timer specifically built to run for long period of time in a single scene,
with a manager and optional autoload to ensure that the state is always safely handled."
author = "NovaDC"
version = "v1.1.0.0"
script = "plugin.gd"
